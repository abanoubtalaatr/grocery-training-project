<footer class="border-t border-gray-100 bg-white mt-auto">

    <div class="px-6 py-4 flex items-center justify-between">

        <p class="text-xs text-gray-400">
            &copy; {{ date('Y') }} Grocery Admin. {{ __('app.all_rights_reserved') }}
.
        </p>

        <div class="flex items-center gap-4 text-xs text-gray-400">
            <a href="#" class="hover:text-gray-600 transition-colors">{{ __('app.privacy') }}</a>
            <a href="#" class="hover:text-gray-600 transition-colors">{{ __('app.terms') }}</a>
            <a href="#" class="hover:text-gray-600 transition-colors">{{ __('app.support') }}</a>
        </div>

    </div>

</footer>
